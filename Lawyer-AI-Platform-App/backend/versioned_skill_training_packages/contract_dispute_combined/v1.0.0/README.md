# 合同纠纷综合训练包 v1.0.0

该组合包用于后续基于真实案件和 legacy rules 的 Skill Training。

组合来源：

* `case-fact-extractor-v3@v1.0.0`
* `case-analysis-pro-v3@v1.0.0`

当前状态：

* 只读。
* `prepared_for_training`。
* 不等于 published skill。
* 不自动训练。
* 不自动发布。
* 不覆盖 `skill_001` 或 `skill_002`。

后续训练输出应生成 candidate skill，例如 `skill_003`。
