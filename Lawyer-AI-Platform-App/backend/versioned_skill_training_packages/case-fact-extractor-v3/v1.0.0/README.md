# 案件事实全面提炼工具 v3 训练包

该包来源于 legacy Codex Skill `case-fact-extractor-v3` 的只读资产。

当前用途：

* 作为后续训练输入。
* 保留材料上下文、证据矩阵、金额核算、矛盾缺口、法律要件映射规则。
* 不自动执行旧脚本。
* 不自动训练。
* 不自动发布。
* 不覆盖 `skill_001` 或 `skill_002`。

事实提炼结果必须由律师复核。
